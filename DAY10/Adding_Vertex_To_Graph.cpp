#include<iostream>
#include<vector>

using namespace std;
int main(){
    vector<vector<int>> adj;
    adj.push_back({});
    adj.push_back({});
    cout<<"Vertices added: "<<adj.size()<<endl;

    //Adding the edges to the graph
    adj[0].push_back(1);

    // Remove the edges from graph
    
}